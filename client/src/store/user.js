// import { create } from "zustand";
// import { client } from "@/lib/utils";

// export const userStore = create((set) => ({
//   user: null,
//   setUser: (user) => set({ user }),
//   getUser: async () => {
//     try {
//       const user = await client("/api/user");
//       set({ user });
//     } catch (error) {
//       console.error(error);
//     }
//   },
// }));
